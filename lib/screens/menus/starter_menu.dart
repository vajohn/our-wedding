import 'package:flutter/material.dart';

class StarterMenu extends StatelessWidget {
  const StarterMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Starter'));
  }
}
