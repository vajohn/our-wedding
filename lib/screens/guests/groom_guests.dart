import 'package:flutter/material.dart';

class GroomGuests extends StatelessWidget {
  const GroomGuests({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Groom'));
  }
}
